import { Then } from "@cucumber/cucumber";
import { InvoicesPage } from "../pages/invoicesPage";

// Then("the invoices page title should be {string}", async function (expectedTitle) {
//   const invoicesPage = new InvoicesPage(this.page);
//   await invoicesPage.verifyTitle(expectedTitle);
// });
